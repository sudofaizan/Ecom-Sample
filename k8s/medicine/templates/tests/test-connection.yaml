apiVersion: v1
kind: Pod
metadata:
  name: medicine-test-connection
  labels:
    app: {{.Values.deployment.name}}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['medicine:80']
  restartPolicy: Never
